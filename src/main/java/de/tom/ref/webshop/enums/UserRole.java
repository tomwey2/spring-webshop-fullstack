package de.tom.ref.webshop.enums;

/**
 * The role of the logged user. It set the permissions.
 */
public enum UserRole {
    /* A normal user role e.g. a customer or a seller. */
    ROLE_USER,
    /* The administrator role. He/She can administrate the system and manipulate the database. */
    ROLE_ADMIN
}
